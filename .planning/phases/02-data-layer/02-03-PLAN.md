---
phase: 2
plan: 3
name: Fix UAT Gaps (Separate Register Pages)
wave: 3
depends_on: [2]
files_modified: [src/app/(qc)/register/layout.tsx, src/app/(qc)/register/cpo/page.tsx, src/app/(qc)/register/pk/page.tsx, src/app/(qc)/register/cangkang/page.tsx, src/app/(qc)/register/actions.ts]
requirements: [R4]
---

# Plan 2-3: Fix UAT Gaps (Separate Register Pages)

## Objective
Address the UAT feedback by splitting the single QC registration form into three distinct pages (`/register/cpo`, `/register/pk`, `/register/cangkang`) and modifying the success alert message to remove the pending terminology.

## Code Patterns (Model Resilience)
Conventions:
- Client Components: Each page is `"use client"` but hardcodes the material type so the user cannot change it via dropdown.
- Shared Layout: Add an optional navigation layout to let operators switch between the 3 material forms.

## Tasks

<task type="auto">
  <name>Refactor Actions Message</name>
  <files>src/app/(qc)/register/actions.ts</files>
  <action>
    1. The previous logic is already correct, but we need to ensure any feedback returned or handled on the client side matches "Vehicle registered successfully."
    2. Adjust `revalidatePath` if needed to cover nested routes (`/register/cpo`, etc).
  </action>
  <verify>npm run build</verify>
  <done>Actions support the new route restructuring.</done>
</task>

<task type="auto">
  <name>Create Separate Form Pages</name>
  <files>src/app/(qc)/register/cpo/page.tsx, src/app/(qc)/register/pk/page.tsx, src/app/(qc)/register/cangkang/page.tsx</files>
  <action>
    1. Delete the existing monolithic `src/app/(qc)/register/page.tsx`.
    2. Create `cpo/page.tsx` with hardcoded material "CPO" and its specific fields (FFA, Moisture).
    3. Create `pk/page.tsx` with hardcoded material "PK" and its specific fields (KA, KK, Stone).
    4. Create `cangkang/page.tsx` with hardcoded material "CANGKANG" and its specific fields (KA, KK, Stone).
    5. Update the success UI state to say EXACTLY: "Vehicle registered successfully." without the string "It is now PENDING in the dashboard".
  </action>
  <verify>npm run build</verify>
  <done>Three separate pages exist and function securely via NextAuth and Prisma.</done>
</task>

## Must-Haves
- 3 distinct URL routes for registration exist.
- The phrase "It is now PENDING in the dashboard" is completely removed.

---
*Created: 2026-04-15*
