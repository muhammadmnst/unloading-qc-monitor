---
phase: 4
plan: 1
name: Public Dashboard Data Access
wave: 1
depends_on: []
files_modified: [src/app/(qc)/dashboard/actions.ts]
requirements: [R12]
---

# Plan 4-1: Public Dashboard Data Access

## Objective
Enable unauthenticated access to dashboard logs for TV displays while maintaining security for mutation actions.

## Code Patterns
- Refactor the existing logic in `getDashboardLogs` into a shared internal function.
- Create a new exported server action `getPublicDashboardLogs` that skips the session check.

## Tasks

<task type="auto">
  <name>Expose Public Data Action</name>
  <files>src/app/(qc)/dashboard/actions.ts</files>
  <action>
    1. Extract the core logic of `getDashboardLogs` into a helper `_fetchDashboardLogs(materialType, showCompleted, completedLimit)`.
    2. Update `getDashboardLogs` to call the helper after checking user session.
    3. Create `getPublicDashboardLogs(materialType, showCompleted, completedLimit)` that calls the helper *without* checking the session.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Public data access is available strictly for fetching logs.</done>
</task>

---
*Created: 2026-04-15*
