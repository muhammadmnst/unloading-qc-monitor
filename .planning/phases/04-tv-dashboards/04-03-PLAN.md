---
phase: 4
plan: 3
name: TV Optimized Data Grids
wave: 2
depends_on: [1, 2]
files_modified: [src/app/tv/cpo/page.tsx, src/app/tv/pk/page.tsx, src/app/tv/cangkang/page.tsx, src/components/tv/tv-table.tsx]
requirements: [R12, R13]
---

# Plan 4-3: TV Optimized Data Grids

## Objective
Create the actual display pages for TVs, optimized for high visibility and standard data density.

## Code Patterns
- Create a shared `TVTable` component that handles polling and auto-pagination.
- Re-use the table structure from operational dashboards but remove action buttons.

## Tasks

<task type="auto">
  <name>Build TV Display Pages</name>
  <files>src/app/tv/cpo/page.tsx, src/app/tv/pk/page.tsx, src/app/tv/cangkang/page.tsx, src/components/tv/tv-table.tsx</files>
  <action>
    1. Create `src/components/tv/tv-table.tsx` with:
       - Auto-polling (reusing 10s logic).
       - Auto-pagination: if the list has more than X items (e.g. 15), rotate through pages every 15 seconds.
       - Clean, oversized header and row styles.
    2. Implement `src/app/tv/cpo/page.tsx` using `TVTable` and `getPublicDashboardLogs`.
    3. Implement `pk` and `cangkang` pages similarly.
  </action>
  <verify>npm run build</verify>
  <done>TV-optimized grids are live for all materials.</done>
</task>

---
*Created: 2026-04-15*
