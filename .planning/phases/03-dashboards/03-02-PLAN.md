---
phase: 3
plan: 2
name: Dashboard Layout Component
wave: 2
depends_on: [1]
files_modified: [src/app/(qc)/dashboard/layout.tsx, src/app/(qc)/dashboard/page.tsx]
requirements: [R9]
---

# Plan 3-2: Dashboard Layout Component

## Objective
Establish the primary URL mapping for the operational dashboard with navigational tabs to separate materials.

## Code Patterns
Conventions:
- Layouts: standard Next.js `layout.tsx` injecting children.

## Tasks

<task type="auto">
  <name>Create Dashboard Navigation Container</name>
  <files>src/app/(qc)/dashboard/layout.tsx, src/app/(qc)/dashboard/page.tsx</files>
  <action>
    1. `src/app/(qc)/dashboard/page.tsx` simply redirects to `/dashboard/cpo`.
    2. `src/app/(qc)/dashboard/layout.tsx` wraps children. Adds a large header "Operational Dashboard" and links for "CPO", "PK", "Cangkang".
  </action>
  <verify>npm run build</verify>
  <done>Dashboard renders visually with sub-routes securely routing.</done>
</task>

## Must-Haves
- Visually distinct tab bar to swap dashboards without re-loading the whole app context.

---
*Created: 2026-04-15*
